<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.patriotbumiandroid">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.Upload.UploadActivity"></activity>
        <activity android:name=".activity.Checkout.CheckoutActivity" />
        <activity android:name=".activity.DonationDetail.DonationDetailActivity" />
        <activity android:name=".activity.Confirm.ConfirmActivity" />
        <activity android:name=".activity.Transfer.TransferActivity" />
        <activity android:name=".activity.Cart.CartActivity" />
        <activity android:name=".activity.ProductDetail.ProductDetailActivity" />
        <activity android:name=".activity.PostAuthenticate.PostMainActivity" />
        <activity android:name=".activity.MyBadge.MyBadgeActivity" />
        <activity android:name=".activity.TodayAction.TodayActionActivity" />
        <activity android:name=".activity.ActivityDetail.ActivityDetailActivity" />
        <activity android:name=".activity.Authenticate.RegisterActivity" />
        <activity android:name=".activity.Authenticate.LoginActivity" />
        <activity android:name=".activity.Article.ArticleListActivity" />
        <activity android:name=".activity.Article.ArticleDetailActivity" />
        <activity android:name=".activity.SplashScreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PreAuthenticate.MainActivity" />
    </application>

</manifest>